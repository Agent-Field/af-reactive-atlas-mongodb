[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash -c 'python setup/seed.py all; echo Starting agent...; exec python -u main.py'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
